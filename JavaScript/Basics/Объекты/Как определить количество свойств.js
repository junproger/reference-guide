'use strict';

const me = {
  name: 'Aleksandra',
  age: 38,
  skills: {
    html: '70%',
    css: '50%',
    js: '10%'
  }
};

// определение количества свойств объекта
console.log(Object.keys(me).length);