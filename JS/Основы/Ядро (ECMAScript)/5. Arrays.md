# Arrays (Массивы)

```js
    // Свойства массива
    length: 0 // длина массива

    // Добавление и удаление элементов в массиве
    push: ƒ push(M) // добавление в конец
    unshift: ƒ unshift(M) // добавление в начало
    pop: ƒ pop(M) // удаление последнего
    shift: ƒ shift(M) // удаление первого

    slice: ƒ slice(I) //  
    splice: ƒ splice(M) //  
    concat: ƒ concat(I)

    // Перебор элементов массива
    forEach: ƒ forEach(I)

    // Преобразование массива
    map: ƒ map(I)
    sort: ƒ sort(I)
    reverse: ƒ reverse(M)
    join: ƒ join(I)
    reduce: ƒ reduce(I)
    flat: ƒ flat(I)
    flatMap: ƒ flatMap(I)
    
    // Поиск по массиву
    indexOf: ƒ indexOf(I)
    lastIndexOf: ƒ lastIndexOf(I)
    includes: ƒ includes(I)
    find: ƒ find(I)
    findIndex: ƒ findIndex(I)
    filter: ƒ filter(I)
    some: ƒ some(I)
    every: ƒ every(I)
    

    constructor: ƒ Array()
    copyWithin: ƒ copyWithin()
    entries: ƒ entries()
    fill: ƒ fill()
    keys: ƒ keys()
    reduceRight: ƒ reduceRight()
    toLocaleString: ƒ toLocaleString()
    toString: ƒ toString()
    values: ƒ values()
    Symbol(Symbol.iterator): ƒ values()
    Symbol(Symbol.unscopables): {copyWithin: true, entries: true, fill: true, find: true, findIndex: true, …}
```